
enum VoucherFilter { active, used, all}
enum VoucherType { tunnelMontBlanc, tunnelFrejus }
enum Direction { italyToFrance, franceToItaly }
enum VehicleType { bus, truck }
enum EuroClass { all,euro3, euro4, euro5, euro6 }
enum VehicleClass { all, class3, class4, classA, classB }
enum Status { active, expired, used }