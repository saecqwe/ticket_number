import 'package:flutter/material.dart';

class AppColors {
  // Primary and secondary colors
  static const Color primaryColor = Color.fromARGB(255, 211, 147, 20);
  static const Color secondaryColor = Colors.blueAccent;

  // Text colors
  static const Color primaryTextColor = Colors.black;
  static const Color secondaryTextColor = Colors.white;
  static const Color bodyTextColor = Color(0xFF000000);

  // Background colors
  static const Color backgroundColor = Colors.white;
  static const Color cardBackgroundColor = Colors.grey;

  // Border colors
  static const Color borderColor = Colors.teal;

  // Other colors
  static const Color redColor = Color(0xFFf72717);
  static const Color greenColor = Color(0xFF00996e);
  static const Color yellowColor = Color(0xFFfcd564);
  static const Color greyColor = Color(0xFF5c6f8a);
  static const Color darkGreyColor = Color(0xFF1f252e);
}
